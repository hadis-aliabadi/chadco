export interface Car {
  make: string;
  model: string;
  min_year: number;
  max_year: number;
  color: string;
  engine: string;
  min_price: number;
  max_price: number;
  fuel_type: string;
  body_style: string;
  min_km: number;
  max_km: number;
}
